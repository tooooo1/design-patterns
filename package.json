{
  "name": "design-patterns",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "bundle": "rm -rf dist && bun build ./index.ts --outdir ./dist"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.3.3"
  }
}
